# 棋盘悬停预览（2026-09-14）

实现范围为前端，不修改引擎、棋谱、实时分析目标。

- 空交叉点显示当前落子颜色的半透明棋子；占用点不显示虚影；移出棋盘清除。
- 棋盘前八候选点及右侧候选列表使用真实分析帧中的 PV，显示交替黑白棋和从 1 开始的编号。
- 预览时隐藏候选圆点、归属色块及原最后一手标记；列表鼠标移开或焦点离开后清除预览。
- 预览在复制的局面上处理提子和 pass；非法占点/越界/自杀时截断，不修改原局面。真实 PV 的合法性由 KataGo 负责，本显示助手不代替完整规则引擎。

验证：TypeScript/Vite 通过；subagent 独立编译及 Node 断言覆盖白方先走、pass 换手/编号、提子、原局面不变、占点终止、自杀回滚，全部通过。
浏览器目视确认空点半透明棋子以及右侧 H3 候选触发的 1–14 手编号预览。此浏览器使用明确标注的真实 KataGo 结果回放。鼠标从格点之间经过时遵循现有 0.42 格命中半径。
