# 菜单交互修复验收（2026-09-14）

用户报告：上方多个菜单可同时展开，点击其他区域后仍停留。

修改范围：`apps/desktop/src/components/LegacyShell.tsx` 的 HTML 菜单交互。后端未修改，本问题不需要 DSH 后端任务。

- 展开互斥；鼠标在已展开菜单之间移动可切换。
- 选择可用命令、外部 pointerdown、Esc、Tab、窗口失焦后关闭。
- 左右键切换顶栏；上下/Home/End 在可用命令间移动；跳过禁用项。
- 悬停切换同步焦点，并避免紧接着点击新菜单时意外关闭。
- 保留现有动作分发、快捷键、testid 和原生菜单入口。

subagent 独立只读审查提出的 Tab 焦点、悬停焦点与 hover-click 时序建议已处理。

## 已验证

- TypeScript、Vite 和 Tauri debug app 打包通过；git diff --check 通过。
- 浏览器真实交互：文件→对局互斥，Esc关闭；重新展开后点标题区域关闭；Down选首个可用项，Right切对局，Tab关闭并聚焦分析菜单；Enter执行载入示例后所有菜单关闭。
- 新打包桌面应用已打开；桌面完整菜单交互验收因用户正在操作原生菜单而停止，未将浏览器验收描述为原生完整验收。
- 窗口失焦及纯悬停路径有实现及代码审查，本轮未单独完成黑盒验证。

应用：`target/debug/bundle/macos/LizzieYzy Next.app`。
持续监工：本任务每小时检查一次，只在新交付、明确回归、完成或阻碍时通知，不打断用户当前界面操作。

## 用户追加：移出自动关闭

用户进一步要求不点击也关闭。新增 mouse pointerleave 150 ms 后收起；pointerenter 取消定时，允许从标题经过小间隙进入下拉选项。触摸不触发悬停收起，组件卸载清除定时器。新构建通过；纯鼠标移出路径待用户当前桌面实际确认。
