# P1 补齐验收记录

## 已实现

1–6：全盘主线自动补齐、粗扫后深算、同任务增量、暂停续跑、复用 local/cloud GTP 通路、手数范围（含前一局面）。
7–12：问题手模块与筛选排序、彩色概览、吻合度与可调口径、黑白平均损失及覆盖率。
13–18：allow/avoid、时间/visits 限额、线程、广度、PDA。

## 自动验证

前端 33 测试通过，涵盖分析视角、真实相邻节点、分支隔离、缺失数据、扫描范围、ACK 前帧缓冲、关闭后的迟到帧、无效预算。
Rust 控制测试及真实参数协议检查详见 P1_ENGINE_CONTROLS_20260914.md / P1_ENGINE_REAL_20260914.json。

## 原生应用观察

新构建应用启动后，默认20手样例自动补齐显示21/21局面。吻合度页黑白均10/10覆盖，上一手掉胜率及目差已有真实数值。截图可见完整0–20手胜率曲线。
范围5–8手，含第4手基线，粗扫与深算显示10/10。再次运行立即完成，未重复等待深算。
较高预算扫描在7/14暂停并保留进度；停止后可重新指定范围。
实时时间上限2秒，顶栏自动变为开始分析，状态显示达到限额、保留连接，计算量冻结。

## 边界

本次原生云端面板未登录，云端全盘扫描及参数接受能力尚未实测；没有把本地协议验证当作云端验收。暂停进度为当前运行会话内保留，不等于退出应用后的持久化续跑。
UI验收发现预算输入即时clamp干扰输入，已改为保留输入字符串、提交时验证。主界面隐藏旧的重复整局控制，兼容底层入口保留。
